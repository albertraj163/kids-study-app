# Prometheus configuration - scrapes metrics from the backend /metrics endpoint
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'kids-study-backend'
    static_configs:
      - targets: ['backend:3000']
    metrics_path: /metrics
